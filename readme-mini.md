Release Notes - v0.76.5

What's new
----------
- Added a History tab (default on launch) showing recent journal events with EDDDiscovery icons and filtering.
- History and icon loading now happens in the background to keep startup responsive.
- Added EDSM sync, that uploads jumps, scans, docking/market/ship events using the commander name/API key from settings.

